<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @make.name %>
</p>

<p>
  <strong>Country:</strong>
  <%= @make.country %>
</p>

<ul>
  <% @car.makes.each do |make| %>
    <li><%= make.name %></li>
 <% end %>
</ul>

<ul>
  <% @make.car.each do |car| %>
    <li><%= c.name %></li>
 <% end %>
</ul>

<%= link_to 'Edit', edit_make_path(@make) %> |
<%= link_to 'Back', makes_path %>
